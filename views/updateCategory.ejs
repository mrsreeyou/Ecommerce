<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Category</title>
    <style>
        /* General styling */
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #74b9ff, #a29bfe);
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        /* Admin Dashboard link styling */
        .header {
            width: 1285px;
            background-color: #0984e3;
            padding: 10px 20px;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            position: absolute;
            top: 0;
            left: 0;
        }

        .header a {
            text-decoration: none;
            color: white;
            font-size: 18px;
            font-weight: bold;
            display: flex;
            align-items: center;
        }

        .header a:hover {
            color: #dbeafe;
        }

        .header a img {
            margin-right: 8px;
            width: 20px;
            height: 20px;
        }

        .add-product-container {
            background: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            width: 400px;
            max-width: 90%;
            text-align: center;
        }

        .add-product-container h1 {
            margin-bottom: 20px;
            font-size: 28px;
            color: #2c3e50;
        }

        .add-product-container form input,
        .add-product-container form button {
            width: 300px;
            padding: 12px 15px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            font-size: 16px;
        }

        .add-product-container form input {
            background: #f5f5f5;
            border: 1px solid #dcdde1;
        }

        .add-product-container form input:focus {
            outline: none;
            border-color: #74b9ff;
            box-shadow: 0 0 5px rgba(116, 185, 255, 0.7);
        }

        .add-product-container form button {
            background: #0984e3;
            color: #fff;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.2s;
        }

        .add-product-container form button:hover {
            background: #74b9ff;
            transform: translateY(-2px);
        }

        .add-product-container form button:active {
            background: #40739e;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .add-product-container {
                width: 90%;
                padding: 20px;
            }

            .add-product-container h1 {
                font-size: 24px;
            }
        }
    </style>
</head>
<body>
    <!-- Admin Dashboard Link -->
    <div class="header">
        <a href="/admin/dashboard">
            <img src="https://img.icons8.com/ios-glyphs/30/ffffff/dashboard.png" alt="Dashboard Icon">
            Admin Dashboard
        </a>
    </div>

    <!-- Add create Form -->
    <div class="add-product-container">
        <h1>Update Category</h1>
            <form method="POST" action="/admin/category/update/<%= category._id %>" >
                <input type="text" name="name" value="<%= category.name %>" required>
                <input type="text" name="description" value="<%= category.description %>" required>
                <button type="submit">create</button>
            </form>
        
    </div>
</body>
</html>
